import * as react from 'react';
import { ReactNode } from 'react';
import { AriaButtonProps } from '@react-aria/button';
import { PressEvent } from '@react-types/shared';

/**
 * Button Component Types
 *
 * Type definitions for the Jio Design System Button component.
 * Based on React Aria button with Design System extensions.
 */

/**
 * Button size variants
 */
type ButtonSize = 'XS' | 'S' | 'M' | 'L' | 'XL';
/**
 * Button attention levels (visual prominence)
 */
type ButtonAttention = 'low' | 'medium' | 'high';
/**
 * Button appearance variants (color themes)
 */
type ButtonAppearance = 'auto' | 'primary' | 'secondary' | 'sparkle' | 'neutral' | 'informative' | 'positive' | 'warning' | 'negative';
/**
 * Button interaction states
 */
type ButtonState = 'idle' | 'hover' | 'pressed' | 'focus' | 'disabled' | 'loading';
/**
 * Design System Button Props
 *
 * Extends React Aria ButtonProps with Design System-specific properties.
 */
interface ButtonProps extends Omit<AriaButtonProps, 'children'> {
    /**
     * Button size variant
     * @default "M"
     */
    size?: ButtonSize;
    /**
     * Visual prominence level
     * @default "medium"
     */
    attention?: ButtonAttention;
    /**
     * Color theme variant
     * @default "auto"
     */
    appearance?: ButtonAppearance;
    /**
     * Whether button has background fill
     * @default true
     */
    contained?: boolean;
    /**
     * Compact spacing (only effective when contained=true)
     * @default false
     */
    condensed?: boolean;
    /**
     * Single button without text (icon or indicator only)
     * When true, aria-label is required
     * @default false
     */
    single?: boolean;
    /**
     * Expand to container width
     * @default false
     */
    fullWidth?: boolean;
    /**
     * Show loading state with progress indicator
     * @default false
     */
    loading?: boolean;
    /**
     * Motion level for animations
     *
     * Controls the intensity of button press animations:
     * - `moderate`: Default motion with scale animation (97% for regular, 99% for full-width, 93% for icon-only)
     * - `subtle`: Reduced motion without scale (respects accessibility preferences)
     *
     * When not provided, automatically detects from user's `prefers-reduced-motion` OS setting.
     * Use this prop primarily for testing or demonstrations in Storybook.
     *
     * @default undefined (auto-detect from prefers-reduced-motion)
     */
    motionLevel?: 'moderate' | 'subtle';
    /**
     * Slot 2 - Start (leading icon or progress indicator)
     * Can be: Icon | CircularProgressIndicator | None
     */
    start?: ReactNode;
    /**
     * Slot 3 - Content (main button content)
     * Can be: Text | Icon | CircularProgressIndicator
     *
     * Note: `children` prop is also supported for backward compatibility,
     * but `content` is the preferred API following the slot system.
     */
    content?: ReactNode;
    /**
     * Slot 4 - End (trailing icon or progress indicator)
     * Can be: Icon | CircularProgressIndicator | None
     */
    end?: ReactNode;
    /**
     * @deprecated Use `content` prop instead to follow the slot system
     * Kept for backward compatibility
     */
    children?: ReactNode;
    /**
     * Custom class name
     */
    className?: string;
    /**
     * Custom inline styles (use sparingly; prefer tokens)
     */
    style?: React.CSSProperties;
    /**
     * Press handler with event details
     * Provides pointer type, coordinates, and modifier keys
     */
    onPress?: (e: PressEvent) => void;
}
/**
 * Button Style Context
 *
 * Context passed to the style resolver for token resolution.
 */
interface ButtonStyleContext {
    /** Button size */
    size: ButtonSize;
    /** Attention level */
    attention: ButtonAttention;
    /** Appearance variant */
    appearance: ButtonAppearance;
    /** Whether button has background */
    contained: boolean;
    /** Compact spacing */
    condensed: boolean;
    /** Single button (no text, icon or indicator only) */
    single: boolean;
    /** Full width */
    fullWidth: boolean;
    /** Current interaction state */
    state: ButtonState;
    /** Surface level from context */
    surfaceLevel: 0 | 1 | 2;
    /** Platform mode */
    platform?: string;
    /** Theme/brand name */
    theme?: string;
    /** Density mode */
    density?: string;
    /** Color mode */
    colorMode?: string;
    /** Is disabled */
    disabled?: boolean;
    /** Is loading */
    loading?: boolean;
    /** Is focused */
    isFocused?: boolean;
    /** Is focus visible (keyboard focus) */
    isFocusVisible?: boolean;
    /** Motion level for animations (moderate or subtle) */
    motionLevel?: 'moderate' | 'subtle';
    /** Whether button is currently pressed (for scale animation) */
    isPressed?: boolean;
}
/**
 * Resolved Button Styles
 *
 * Style objects for each slot, resolved from tokens.
 */
interface ButtonStyles {
    /** Root button element styles */
    root: React.CSSProperties;
    /** Start slot styles */
    start?: React.CSSProperties;
    /** Content slot styles */
    content: React.CSSProperties;
    /** End slot styles */
    end?: React.CSSProperties;
}

/**
 * Button Component
 */
declare const Button: react.ForwardRefExoticComponent<ButtonProps & react.RefAttributes<HTMLButtonElement>>;

export { Button as B, type ButtonAppearance as a, type ButtonAttention as b, type ButtonProps as c, type ButtonSize as d, type ButtonState as e, type ButtonStyleContext as f, type ButtonStyles as g };
