import { e as ButtonState, f as ButtonStyleContext, g as ButtonStyles } from '../../Button-BYZDmT8U.mjs';
export { B as Button, a as ButtonAppearance, b as ButtonAttention, c as ButtonProps, d as ButtonSize, B as default } from '../../Button-BYZDmT8U.mjs';
import 'react';
import '@react-aria/button';
import '@react-types/shared';

/**
 * Button Styles - Token Resolution
 *
 * Resolves all button styles from design tokens.
 *
 * CRITICAL RULES:
 * - NO hard values (hex/px/ms/etc.)
 * - ALL styles must come from token resolver
 * - Surface-aware styling required
 * - State-based token resolution
 *
 * This file will FAIL linting if any literals are detected.
 */

/**
 * Resolve all button styles from tokens
 */
declare function resolveButtonStyles(context: ButtonStyleContext): ButtonStyles;
/**
 * Determine current button state from props and interaction state
 */
declare function determineButtonState(isPressed: boolean, isHovered: boolean, isFocused: boolean, isDisabled: boolean, isLoading: boolean): ButtonState;

export { ButtonState, ButtonStyleContext, ButtonStyles, determineButtonState, resolveButtonStyles };
